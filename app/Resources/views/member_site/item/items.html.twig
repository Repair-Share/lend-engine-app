{% extends 'member_site/themes/loader.html.twig' %}
{% trans_default_domain 'member_site' %}

{% block body %}
    {% if is_granted('ROLE_ADMIN') and app.request.get('tagId') %}
        <div style="margin-bottom: 10px; text-align: right" class="pull-right">
            <a target="_blank" href="{{ path('printable_catalogue', {category: app.request.get('tagId'), columns: 2}) }}">Print this category</a>
        </div>
    {% endif %}
    {{ include('member_site/item/items_core.html.twig') }}
{% endblock %}