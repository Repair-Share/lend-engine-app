{% extends 'shared/modal.html.twig' %}
{% trans_default_domain 'member_site' %}

{% block subTitle %}
Move or service item:
{% endblock %}

{% block title %}
    <strong>{{ title }}</strong>
{% endblock %}

{% block body %}

    {{ form_start(form) }}
    {{ form_errors(form) }}
    {% if help is defined %}
        <span class="help">{{ help }}</span>
    {% endif %}

    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.location) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.maintenancePlan) }}
        </div>
    </div>

    <div class="help-block">
        In order to create maintenance or service tasks; you'll need to have created one or more maintenance types first.<br>
        If the maintenance type has a provider, they will be emailed when maintenance is scheduled.
    </div>

    <div class="row">
        <div class="col-md-12">
            {{ form_row(form.notes) }}
        </div>
    </div>

    <input type="hidden" name="form_action" value="item_move">
    <input type="hidden" name="idSet" value="{{ idSet }}">

    {{ form_end(form) }}

{% endblock %}

{% block footer %}

    <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Cancel</button>
    <button class="modal-submit btn btn-success pull-right">Move / service item</button>

{% endblock %}